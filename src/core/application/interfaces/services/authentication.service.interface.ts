// import {Session} from "../../../domain/models/session";
// import {UserSelectSchema} from "../../../domain/models/user";
// import {Cookie} from "../../../domain/models/cookie";

// export interface IAuthenticationService {
//     generateUserId(): string;
//     validateSession(
//         sessionId: Session["id"],
//     ): Promise<{ user: UserSelectSchema; session: Session }>;
//     createSession(user: UserSelectSchema): Promise<{ session: Session; cookie: Cookie }>;
//     invalidateSession(sessionId: Session["id"]): Promise<{ blankCookie: Cookie }>;
// }
